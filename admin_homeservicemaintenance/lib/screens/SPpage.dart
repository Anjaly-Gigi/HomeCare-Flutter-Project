import 'package:flutter/material.dart';

class ManageSP extends StatelessWidget {
  const ManageSP({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Service Provider"),);
  }
}